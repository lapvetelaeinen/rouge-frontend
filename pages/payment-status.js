import StripePaymentStatus from '../components/payment/stripe/paymentStatus'

const PaymentStatus = () => {

    return (
        <div>
            <StripePaymentStatus />
        </div>
    );
}

export default PaymentStatus;
